# Tools

Host-side tools will automate identity capture, firmware hashing, acceptance manifests and evidence folder generation.

A future `run_hw01_identity.py` should follow the evidence-oriented workflow used in `esp32-2432s028-lab`, adapted for ESP32-S3-class identification and this repository's specimen/profile schema.
