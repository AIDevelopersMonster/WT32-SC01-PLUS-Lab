# Libraries

A reusable `WT32_SC01_PLUS_Board` support library should be created only after the Panlee reference specimen has a verified profile. Keep variant-specific pin maps outside application examples where possible.
